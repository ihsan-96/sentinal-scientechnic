# Base URL of the backend API. The SSE stream is derived as ${VITE_API_URL}/stream.
# Local dev points at the Dockerized backend; on Netlify/Vercel set this to the
# deployed backend URL in the project's environment variables.
VITE_API_URL=http://localhost:4000/api
