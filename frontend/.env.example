# Base URL of the backend API. The SSE stream is derived as ${VITE_API_URL}/stream.
# Defaults to the local Docker backend (below). On Netlify/Vercel set this to the deployed
# backend URL (e.g. https://scientechnic.madian.in/api) in the project's environment variables.
VITE_API_URL=http://localhost:4000/api
