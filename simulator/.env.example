# Default backend endpoint shown in the UI (editable at runtime in the app).
# Local dev points at the Dockerized backend; on Netlify/Vercel set the deployed URL.
VITE_API_URL=http://localhost:4000/api
